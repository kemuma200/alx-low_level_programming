#include "main.h"
/**
 *even_sum_fibonacci - prints sum of even fib numbers
 *@num1: first number
 *@num2: second number
 *@sum: summation of fin numbers
 *@max_digit: Maximu value of fib number
 *Return: Always (0)
 */
int even_sum_fibonacci(void)
{
	unsigned long num1 = 0, num2 = 1, sum = 0;
	max_digit = 4000000;

	while (num2 < 4000001)
	{
		if ((num1 % 2) == 0)
			sum += num1;
		if ((num2 % 2) == 0)
			sum += num2;
		num1 = first_number;
		num1 = num2;
		num2 += first_number;
	}
	printf("%.0f\n", sum);
	return (0);
}
