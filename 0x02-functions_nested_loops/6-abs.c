include "main.h"
/**
 * _abs - computes absolute value of an integer
 * @p :The integer considered
 *
 * Return: the absolute value of an integer
 */
int _abs(int p)
{
	if (p >= 0)
		return (p);
	else
		return (-p);
}
