#include "main.h"

/**
 *_puts_recursion - prints cahracter followed by a new line
 *@s: character to eb printed
 *
 */

void _puts_recursion(char *s)
{
	if(*s == 0)
	{
	  _putchar('\n');
	}
	_putchar(*s);
	_puts_recursion(s + 1);
}

