#include "main.h"
#include "unistd.h"

/**
 *_putchar - writes character to stdout
 *@c: character to be printed
 *Return: 1 0n success, -1 on error
 *
 */

int _putchar(char c)
{
	return (write(1, &c, 1));
}
