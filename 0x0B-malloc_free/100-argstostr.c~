#include "main.h"

/**
 *
 *
 *
 *
 */
char *argstostr(int ac, char **av)
{
  int i, j, k, len;
  char *p;

  if (ac == NULL || av == NULL)
    return (NULL);

  for (i = 0; i < ac; i++)
    {
      for (j = 0; av[i][j] != '\0'; j++)
	{
	  len++;
	}
      len++;
    }
  len++;
  
}
